using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;

namespace FD_STOCK
{
    static class Program
    {
        /// <summary>
        /// Point d'entrée principal de l'application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            // Application.Run(new np());
            // Application.Run(new lp());
            //  Application.Run(new nc());
            // Application.Run(new lc());
            // Application.Run(new nf());
            // Application.Run(new lf());
            // Application.Run(new ne());
            // Application.Run(new le());
            // Application.Run(new ns());
            // Application.Run(new ls());
            // Application.Run(new nm());
            // Application.Run(new lm());
            // Application.Run(new nem());
            //Application.Run(new lem());
            // Application.Run(new nsm());
            // Application.Run(new lsm());
            // Application.Run(new nemb());
            // Application.Run(new lemb());
            // Application.Run(new neemb());
            // Application.Run(new leemb());
            // Application.Run(new nsemb());
            // Application.Run(new lsem());
            // Application.Run(new MDIParent1());
            // Application.Run(new menu());
           //  Application.Run(new menur());
            Application.Run(new conx());
            //Application.Run(new utilisateur());
            //Application.Run(new listePieceRechange());
            // Application.Run(new nmach());
            // Application.Run(new lmach());
            // Application.Run(new dmach());
            // Application.Run(new nart());
            //Application.Run(new la());

            // Application.Run(new prech());
            // Application.Run(new ca());
            // Application.Run(new cas());
            // Application.Run(new pers());
            // Application.Run(new dpers());
            //Application.Run(new lpers());
            // Application.Run(new ldpers());
            //  Application.Run(new eg());
            //Application.Run(new regl());
            //Application.Run(new npg());
            // Application.Run(new lpg());
            // Application.Run(new sg());
            // Application.Run(new ecc());
            //Application.Run(new scc());
        }
    }
}
